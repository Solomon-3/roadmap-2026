we are now organizing our code rather than everything being in server.js 

so
server.js, routes(users.js, products.js), middleware(auth.js)

so as we can see we now have 6 items only ie these 3 and the nodemodules, package.json, package-lock.json    ...............totalling to 6 items(controllers and services are missing)

flow
server.js -> users router -> authMiddleware -> route handler -> JSON response

now try adding products 



DAY 3
REQUEST ie POST
The API must 1) parse the request body, 2) store the data in memory

1) enable json parsing
'app.use(express.json());'
without it -> 'req.body = undefined'
withit -> 'req.body = parsed JSON object'

2)add post route for users
mind you the value added is always in 'req.body'

know the diff between 
req.query.key
req.params.id
req.body.name

3) GET /users/2
GET /users:id




DAY 4
one of the problems we have so far is that in the event that user 2 is deleted , the IDs of the rest dont change eg 1,3,4 if one is to add another person then lem(users)+1 => 1,3,4,4
as you can see we now have two users with id 4 yet ids must be unique,
APIs depend on ids for updates, deletes, linking data


